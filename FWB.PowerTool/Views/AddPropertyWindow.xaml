<Window x:Class="FWB.PowerTool.Views.AddPropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Property" Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D2D">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Tabs -->
        <TabControl Grid.Row="0" Background="#FF333333" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="0" Background="Transparent" Height="40" MinWidth="120">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="20,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF2D2D2D"/>
                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#FF333333"/>
                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="Gray"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </TabControl.ItemsPanel>

            <TabItem Header="Property Info" IsSelected="True">
                <!-- Content -->
                <Grid Grid.Row="1" Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Side -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <GroupBox Header="Property Info" Foreground="White" Background="#FF333333" Margin="0,0,0,10">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Property Type" Foreground="White" Grid.Row="0" VerticalAlignment="Center"/>
                                <ComboBox x:Name="PropertyTypeComboBox" Grid.Row="0" Grid.Column="1" Margin="10,5" Background="#FF2D2D2D" Foreground="#FF2D2D2D" SelectionChanged="PropertyTypeComboBox_SelectionChanged">
                                    <ComboBoxItem Foreground="#FF2D2D2D" Content="string"/>
                                    <ComboBoxItem Content="bool"/>
                                    <ComboBoxItem Content="byte"/>
                                    <ComboBoxItem Content="short"/>
                                    <ComboBoxItem Content="DateTime"/>
                                    <ComboBoxItem Content="decimal"/>
                                    <ComboBoxItem Content="double"/>
                                    <ComboBoxItem Content="Guid"/>
                                    <ComboBoxItem Content="int"/>
                                    <ComboBoxItem Content="long"/>
                                    <ComboBoxItem Content="enum"/>
                                    <ComboBoxItem Content="file"/>
                                </ComboBox>

                                <TextBlock Text="Property Name" Foreground="White" Grid.Row="1" VerticalAlignment="Center"/>
                                <TextBox x:Name="PropertyNameTextBox" Grid.Row="1" Grid.Column="1" Margin="10,5" Background="#FF2D2D2D" Foreground="White"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Others" Foreground="White" Background="#FF333333">
                            <StackPanel Margin="10">
                                <CheckBox x:Name="AdvancedFilterCheckBox" Content="Advanced Filter" Foreground="White" Margin="0,2"/>
                                <CheckBox x:Name="ListInUICheckBox" Content="List in UI" Foreground="White" Margin="0,2"/>
                                <CheckBox x:Name="ListInMauiCheckBox" Content="List in Maui" Foreground="White" Margin="0,2"/>
                                <CheckBox x:Name="CreateAndUpdateCheckBox" Content="Create And Update" Foreground="White" Margin="0,2"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>

                    <!-- Right Side -->
                    <GroupBox Header="Attributes" Grid.Column="1" Foreground="White" Background="#FF333333">
                        <StackPanel Margin="10">
                            <Grid Margin="0,0,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Min-Max Length" Foreground="White" Grid.Row="0" VerticalAlignment="Center"/>
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="10,5">
                                    <TextBox x:Name="MinLengthTextBox" Width="60" Background="#FF2D2D2D" Foreground="White"/>
                                    <TextBlock Text=" - " Foreground="White" VerticalAlignment="Center" Margin="5,0"/>
                                    <TextBox x:Name="MaxLengthTextBox" Width="41" Background="#FF2D2D2D" Foreground="White" RenderTransformOrigin="0.293,0.561"/>
                                </StackPanel>

                                <TextBlock Text="Regular Expression" Foreground="White" Grid.Row="1" VerticalAlignment="Center"/>
                                <TextBox x:Name="RegularExpressionTextBox" Grid.Row="1" Grid.Column="1" Margin="10,5" Background="#FF2D2D2D" Foreground="White"/>
                            </Grid>

                            <CheckBox x:Name="RequiredCheckBox" Content="Required" Foreground="White" Margin="0,2"/>
                            <CheckBox x:Name="NullableCheckBox" Content="Nullable" Foreground="White" Margin="0,2"/>
                            <CheckBox x:Name="MultiLingualPropertyCheckBox" Content="Multi Lingual Property" Foreground="White" Margin="0,2"/>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>
            <TabItem Header="Attributes"></TabItem>
            <TabItem Header="Navigation Properties"></TabItem>
        </TabControl>

       

        <!-- Footer -->
        <Grid Grid.Row="2" Background="#FF2D2D2D">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10">
                <Button Content="Cancel" Width="100" Height="30" Margin="0,0,10,0" Click="CancelButton_Click"
                        Background="#FF424242" Foreground="White"/>
                <Button Content="OK" Width="100" Height="30" Click="OKButton_Click"
                        Background="#FF00B050" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
