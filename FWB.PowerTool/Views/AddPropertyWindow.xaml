<Window x:Class="FWB.PowerTool.Views.AddPropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FWB.PowerTool.Views"
        xmlns:vm="clr-namespace:FWB.PowerTool.ViewModels"
        mc:Ignorable="d"
        Title="Property" Height="350" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D2D">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FWB.PowerTool;component/Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Content -->
        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <GroupBox Header="Property Info" BorderThickness="0" Foreground="White" Background="#535354" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Property Type" Foreground="White" Grid.Row="0" VerticalAlignment="Center"/>
                        <ComboBox x:Name="PropertyTypeComboBox" 
                                Height="30" 
                                Grid.Row="0" 
                                Grid.Column="1" 
                                Margin="10,5" 
                                Background="#FF2D2D2D" 
                                Foreground="White"
                                ItemsSource="{Binding PropertyTypes}"
                                SelectedItem="{Binding PropertyType}"
                                SelectedIndex="0">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="#FF2D2D2D"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <TextBlock Text="Property Name" Foreground="White" Grid.Row="1" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Height="30" Margin="10,5"
                                Text="{Binding PropertyName, UpdateSourceTrigger=PropertyChanged}"
                                Background="White" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="Others" BorderThickness="0" Foreground="White" Background="#535354">
                    <StackPanel Margin="10">
                        <CheckBox Content="Advanced Filter" Foreground="White" IsChecked="{Binding IsAdvancedFilter}"/>
                        <CheckBox Content="Create And Update" Foreground="White" IsChecked="{Binding IsCreateAndUpdate}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>

            <!-- Right Side -->
            <GroupBox Header="Attributes" Grid.Column="1" BorderThickness="0" Foreground="White" Background="#535354">
                <StackPanel Margin="10">
                    <StackPanel>
                        
                    </StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Min-Max Length" Grid.Row="0" Grid.Column="0" Foreground="White" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Height="30" Margin="10,5"
                                 Text="{Binding MinLength}"
                                 Background="White"/>
                        <TextBlock Text="-" Grid.Row="0" Grid.Column="2" Foreground="White" VerticalAlignment="Center" Margin="5,0"/>
                        <TextBox Grid.Row="0" Grid.Column="3" Height="30" Margin="10,5"
                                 Text="{Binding MaxLength}"
                                 Foreground="White"/>

                        <CheckBox Content="Required" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" 
                                  Margin="0,10,0,0" Foreground="White" IsChecked="{Binding IsRequired}"/>
                        <CheckBox Content="Nullable" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" 
                                  Margin="0,10,0,0" Foreground="White" IsChecked="{Binding IsNullable}"/>
                    </Grid>

                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="1" Background="#FF2D2D2D">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10">
                <Button Content="Cancel" Width="100" Height="30" Margin="0,0,10,0" Command="{Binding CancelCommand}"
                        Style="{StaticResource GrayButtonStyle}"/>
                <Button Content="OK" Width="100" Height="30" Command="{Binding SaveCommand}"
                        Style="{StaticResource GreenButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
