<UserControl x:Class="FWB.PowerTool.Views.PropertiesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <!-- Header with Property Type and Name -->
        <Grid Background="#FF333333" Height="40" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Property Type" Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <TextBlock Grid.Column="1" Text="Property Name" Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <Button Grid.Column="2" Content="ADD NEW PROPERTY" 
                    Background="#FF00B050" Foreground="White" 
                    Padding="15,5" Margin="10,5" 
                    BorderThickness="0"
                    x:Name="AddPropertyButton" Click="AddPropertyButton_Click"/>
        </Grid>

        <!-- Properties List -->
        <ScrollViewer Margin="0,40,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PropertiesList" Background="#FF2D2D2D">
                <!-- Properties will be added here dynamically -->
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
